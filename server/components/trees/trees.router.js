'use strict';

var express = require('express');

var auth = require('../../lib/auth');
var userEndpoints = require('./trees.endpoints');


/**
 * Trees router
 */

module.exports = function () {
    var router = express.Router();

    return router;
};
